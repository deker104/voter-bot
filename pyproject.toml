[project]
name = "vote-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiogram==3.*",
    "aiosqlite>=0.22.1",
]

[dependency-groups]
dev = [
    "ipython>=9.9.0",
]
